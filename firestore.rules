rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper Functions
    function isAuthenticated() {
      return request.auth != null;
    }

    function getUserData() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }

    function isMaster() {
      return isAuthenticated() && (
        request.auth.token.email == "faustodv@gmail.com" ||
        request.auth.token.email == "multprofissoesoficial@gmail.com" ||
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'master')
      );
    }

    function isCoordenador(franquiaId) {
      return isAuthenticated() && exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'coordenador' && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.franquiaId == franquiaId;
    }

    function isProfessor(franquiaId) {
      return isAuthenticated() && exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'professor' && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.franquiaId == franquiaId;
    }

    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    // Franquias
    match /franquias/{franquiaId} {
      allow read: if isAuthenticated();
      allow write: if isMaster();
    }

    // Usuários
    match /users/{userId} {
      allow read: if isAuthenticated() && (
        isOwner(userId) || 
        isMaster() || 
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'coordenador' && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.franquiaId == resource.data.franquiaId) ||
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'professor' && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.franquiaId == resource.data.franquiaId)
      );
      allow create: if isMaster() || (isAuthenticated() && exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'coordenador' && request.resource.data.franquiaId == get(/databases/$(database)/documents/users/$(request.auth.uid)).data.franquiaId);
      allow update: if isMaster() || 
                    (isAuthenticated() && exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'coordenador' && resource.data.franquiaId == get(/databases/$(database)/documents/users/$(request.auth.uid)).data.franquiaId) ||
                    (isAuthenticated() && exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'professor' && resource.data.franquiaId == get(/databases/$(database)/documents/users/$(request.auth.uid)).data.franquiaId) ||
                    isOwner(userId);
      allow delete: if isMaster();
    }

    // Missões (Diários)
    match /missions/{missionId} {
      allow read: if isAuthenticated() && (
        isMaster() || 
        resource.data.studentId == request.auth.uid ||
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.franquiaId == resource.data.franquiaId && (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'coordenador' || get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'professor'))
      );
      allow create: if isAuthenticated() && request.resource.data.studentId == request.auth.uid;
      allow update: if isMaster() || (isAuthenticated() && exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.franquiaId == resource.data.franquiaId && (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'coordenador' || get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'professor'));
      allow delete: if isMaster();
    }
  }
}
